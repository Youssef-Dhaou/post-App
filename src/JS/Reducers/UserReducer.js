const initialState = { users: [] };
const UserReducer = (state= initialState,{type, payload}) => {
    switch (type) {
        case "": 
        break;
        
    
        default: 
        return state;
    }
}

export default UserReducer